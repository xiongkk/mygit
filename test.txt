$folder = "D:\COBOL\src"

Get-ChildItem -Path $folder -File -Include *.cbl,*.cpy | ForEach-Object {
    $content = Get-Content $_.FullName -Raw
    $newContent = $content -replace '(?m)^.{6}', ''
    Set-Content $_.FullName $newContent -Encoding Default
}